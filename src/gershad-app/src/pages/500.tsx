import { PageComponent } from "src/utils/createLocalePageComponent";

// Placeholder page to avoid build errors — won’t ever be rendered
const ErrorPage: PageComponent = () => {
  return <></>;
};

export default ErrorPage;
